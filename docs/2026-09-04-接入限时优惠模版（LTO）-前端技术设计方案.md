# 素材模版限时优惠 LTO 前端技术方案

- 文档日期：2026-09-04
- 关联 PRD：[2026-09-04-素材模版限时优惠LTO-PRD.md](2026-09-04-接入限时优惠模版（LTO）-需求文档.md)
- 范围：素材模板页 LTO 配置、人工组合 LTO 选择、相关接口接入
- 原则：复用现有素材交互；后端负责时间转换、策略校验和正式模板下发

---

# 1. 页面展示与交互

## 1.1 素材模板页

在【模板头部】和【模板内容】之间新增【限时优惠】区域。

### 无 LTO 素材

```text
┌────────────────────────────────────────────────────────────┐
│ 模板头部                                             [添加] │
└────────────────────────────────────────────────────────────┘

┌────────────────────────────────────────────────────────────┐
│ 限时优惠                                                   │
│                                                            │
│ 暂无限时优惠素材。新增后可在人工组合中选择使用。                  │
│                                                 [ + 添加 ] │
└────────────────────────────────────────────────────────────┘

┌────────────────────────────────────────────────────────────┐
│ * 模板内容                                             [添加] │
└────────────────────────────────────────────────────────────┘
```

### 多个 LTO 素材

```text
┌────────────────────────────────────────────────────────────┐
│ 限时优惠                                             [添加] │
│                                                            │
│ ┌────────────────────────────────────────────────────────┐ │
│ │ 限时优惠 LTO-01                      [预览][停止生成][停用] │ │
│ │ 优惠文案：Offer ends soon            16/16               │ │
│ │ 展示到期时间：开启                                      │ │
│ │ 到期方式：发送后 30 分钟                                 │ │
│ └────────────────────────────────────────────────────────┘ │
│                                                            │
│ ┌────────────────────────────────────────────────────────┐ │
│ │ 限时优惠 LTO-02                      [预览][停止生成][停用] │ │
│ │ 优惠文案：Flash Sale                 10/16               │ │
│ │ 展示到期时间：开启                                      │ │
│ │ 到期方式：2026-09-10 12:00 Asia/Jakarta                │ │
│ └────────────────────────────────────────────────────────┘ │
└────────────────────────────────────────────────────────────┘
```

## 1.2 LTO 编辑表单

### RELATIVE 示例

```text
优惠文案：[Offer ends soon]                                16/16

展示到期时间：[ON]

到期方式：
(●) 相对时间
( ) 绝对时间

发送后：[30] 分钟过期
```

### ABSOLUTE 示例

```text
优惠文案：[Flash Sale]                                     10/16

展示到期时间：[ON]

到期方式：
( ) 相对时间
(●) 绝对时间

到期时间：[2026-09-10 12:00]
时区：    [Asia/Jakarta]
```

字段规则：

| 字段 | 控件 | 必填 | 规则 |
| --- | --- | --- | --- |
| 优惠文案 | Input | 是 | 最大 16 字符 |
| 展示到期时间 | Switch | 是 | 关闭时隐藏策略字段 |
| 到期方式 | Radio | 条件必填 | `hasExpiration=true` 时展示 |
| 绝对到期时间 | DateTimePicker | 条件必填 | 必须晚于当前时间 |
| 时区 | Select | 条件必填 | 必须选择 |
| 相对时长 | InputNumber + Select | 条件必填 | 大于 0 |

时区默认值：

| 国家 | 默认时区 |
| --- | --- |
| 印尼 | `Asia/Jakarta` |
| 尼日利亚 | `Africa/Lagos` |

## 1.3 LTO 卡片操作

| 操作 | 行为 |
| --- | --- |
| 预览 | 右侧展示优惠文案、到期时间或倒计时，不调用发送接口（和当前预览效果一样） |
| 停止生成 | 复用现有 BODY 素材的停止生成逻辑 |
| 停用 | 复用现有素材停用逻辑；停用后不能被新组合选择 |

## 1.4 人工组合页

在【头部】和【内容】之间新增【限时优惠】下拉。

### 无 LTO 组合

```text
#1
* 模板类型：普通模板
头部：无HEADER
限时优惠：无 LTO
* 内容：内容
脚部：无FOOTER
```

### LTO 组合

```text
#1  [限时优惠]
* 模板类型：普通模板
头部：无HEADER
限时优惠：LTO-30分钟
* 内容：内容
脚部：无FOOTER（置灰）
```

### 联动规则

| 事件 | 前端处理 |
| --- | --- |
| 选择“无 LTO” | 恢复 FOOTER 可选，按普通组合校验 |
| 选择某个 LTO | FOOTER 清空为 `无FOOTER` 并置灰 |
| LTO `hasExpiration=true` | 校验当前按钮素材第一个按钮是 `COPY_CODE` |
| LTO `hasExpiration=false` | 按普通按钮逻辑校验；如有 COPY_CODE 仍需第一位 |
| LTO 已停用 | 下拉不可选，保存时报错 |

### 组合预览

```text
┌────────────────────────────────────┐
│ Offer ends soon                    │
│ ⏳ 有效期至：2026-09-10 12:00       │
│                                   │
│ Hi Tom, use code SAVE20           │
│ before it expires.                │
│                                   │
│ [Copy offer code]                 │
│ [Book now]                        │
└────────────────────────────────────┘
```

预览规则：

| 类型 | 预览展示 |
| --- | --- |
| ABSOLUTE | 展示 `expireAt + timezoneId` |
| RELATIVE | 展示“发送后 30 分钟内有效”，或本地模拟 `Date.now() + durationMillis` |

---

# 2. 前端组件与状态

## 2.1 组件拆分

| 组件 | 作用 |
| --- | --- |
| `LtoMaterialSection` | 素材模板页 LTO 区域容器 |
| `LtoMaterialCard` | 单个 LTO 素材卡片 |
| `LtoMaterialForm` | LTO 新增 / 编辑表单 |
| `LtoPreviewModal` | LTO 预览弹窗 |
| `ComboLtoSelect` | 人工组合页 LTO 下拉 |
| `ComboTemplatePreview` | 组合模板预览，增加 LTO 渲染区域 |

## 2.2 核心状态

```ts
interface LtoExpirationPolicy {
  type: 'ABSOLUTE' | 'RELATIVE';
  expireAt?: string;
  expireAtMs?: number;
  timezoneId?: string;
  countryCode?: string;
  sourceExpression?: string;
  durationMillis?: number;
}

interface LtoElement {
  type: 'LIMITED_TIME_OFFER';
  limited_time_offer: {
    text: string;
    has_expiration: boolean;
    expirationPolicy?: LtoExpirationPolicy;
  };
}

interface LtoMaterialItem {
  tempKey?: string;
  id?: string;
  materialGroupId?: number;
  status: 'ACTIVE' | 'PAUSED' | 'DELETED';
  type: 'LIMITED_TIME_OFFER';
  subType: 'NONE';
  element: LtoElement;
}
```

新增 LTO 时，前端生成本地 `tempKey`，用于同一请求内新建素材和新建组合的临时引用：

```ts
const tempKey = `lto_temp_${Date.now()}_${Math.random().toString(16).slice(2)}`;
```

人工组合状态：

```ts
interface ManualCombination {
  templateType: 'NORMAL';
  headerMaterialRef?: string;
  limitedTimeOfferMaterialRef?: string;
  bodyMaterialRef: string;
  footerMaterialRef?: string;
  buttonsMaterialRef?: string;
  status: 'ACTIVE' | 'INACTIVE';
}
```

---

# 3. 接口与参数示例

## 3.1 素材模板创建 / 编辑

接口：

```http
POST /admin/api/material/create
POST /admin/api/material/update
```

请求示例：

```json
{
  "name": "promotion_material_group",
  "templateType": "MARKETING",
  "language": "en",
  "status": "ENABLED",
  "headerList": [],
  "limitedTimeOfferList": [
    {
      "tempKey": "lto_temp_1",
      "status": "ACTIVE",
      "type": "LIMITED_TIME_OFFER",
      "subType": "NONE",
      "element": {
        "type": "LIMITED_TIME_OFFER",
        "limited_time_offer": {
          "text": "Offer ends soon",
          "has_expiration": true,
          "expirationPolicy": {
            "type": "RELATIVE",
            "durationMillis": 1800000
          }
        }
      }
    }
  ],
  "bodyList": [
    {
      "tempKey": "body_temp_1",
      "status": "ACTIVE",
      "type": "BODY",
      "subType": "TEXT",
      "element": {
        "type": "BODY",
        "text": "Hi {{1}}, use code {{2}} before it expires.",
        "example": {
          "body_text": [["Tom", "SAVE20"]]
        }
      }
    }
  ],
  "footerList": [],
  "buttons": {
    "tempKey": "buttons_temp_1",
    "status": "ACTIVE",
    "type": "BUTTONS",
    "element": {
      "type": "BUTTONS",
      "buttons": [
        {
          "type": "COPY_CODE",
          "example": "SAVE20"
        }
      ]
    }
  },
  "combinationMode": "MANUAL",
  "manualCombinations": [
    {
      "headerMaterialRef": null,
      "limitedTimeOfferMaterialRef": "lto_temp_1",
      "bodyMaterialRef": "body_temp_1",
      "footerMaterialRef": null,
      "buttonsMaterialRef": "buttons_temp_1",
      "status": "ACTIVE"
    }
  ]
}
```

响应示例：

```json
{
  "code": 0,
  "message": "success",
  "data": 1001
}
```

## 3.2 素材模板详情

接口：

```http
POST /admin/api/material/groupDetail
```

请求：

```json
{
  "id": 1001
}
```

响应新增：

```json
{
  "data": {
    "id": 1001,
    "limitedTimeOfferList": [
      {
        "id": "66f1a2b3c4d5e6f7a8b9c0d1",
        "materialGroupId": 1001,
        "status": "ACTIVE",
        "type": "LIMITED_TIME_OFFER",
        "subType": "NONE",
        "element": {
          "type": "LIMITED_TIME_OFFER",
          "limited_time_offer": {
            "text": "Offer ends soon",
            "has_expiration": true,
            "expirationPolicy": {
              "type": "RELATIVE",
              "durationMillis": 1800000
            }
          }
        }
      }
    ],
    "manualCombinations": [
      {
        "limitedTimeOfferMaterialRef": "66f1a2b3c4d5e6f7a8b9c0d1"
      }
    ]
  }
}
```

## 3.3 人工组合查询

接口：

```http
POST /admin/api/material/combination/page
GET  /admin/api/material/combination/findTaskCombinationInfo
```

响应新增：

```json
{
  "limitedTimeOfferMaterialId": "66f1a2b3c4d5e6f7a8b9c0d1",
  "limitedTimeOfferMaterial": {
    "id": "66f1a2b3c4d5e6f7a8b9c0d1",
    "materialGroupId": 1001,
    "type": "LIMITED_TIME_OFFER",
    "status": "ACTIVE",
    "element": {
      "type": "LIMITED_TIME_OFFER",
      "limited_time_offer": {
        "text": "Offer ends soon",
        "has_expiration": true,
        "expirationPolicy": {
          "type": "RELATIVE",
          "durationMillis": 1800000
        }
      }
    }
  }
}
```

无 LTO 组合：

```json
{
  "limitedTimeOfferMaterialId": "",
  "limitedTimeOfferMaterial": null
}
```

## 3.4 模板创建

接口：

```http
POST /admin/api/messageTemplate/create
```

`components` 新增 LTO 组件：

```json
{
  "name": "limited_time_offer_2026",
  "language": "en_US",
  "category": "MARKETING",
  "components": [
    {
      "type": "LIMITED_TIME_OFFER",
      "limited_time_offer": {
        "text": "Offer ends soon",
        "has_expiration": true,
        "expirationPolicy": {
          "type": "RELATIVE",
          "durationMillis": 1800000
        }
      }
    },
    {
      "type": "BODY",
      "text": "Hi {{1}}, use code {{2}} before it expires.",
      "example": {
        "body_text": [
          ["Tom", "SAVE20"]
        ]
      }
    },
    {
      "type": "BUTTONS",
      "buttons": [
        {
          "type": "COPY_CODE",
          "example": "SAVE20"
        }
      ]
    }
  ],
  "selectedAgentAndWabaIds": [
    {
      "agent": "YCLOUD",
      "wabaId": "waba-001"
    }
  ]
}
```

## 3.5 现有素材操作

【停止生成】和【停用 / 启用】复用现有素材操作逻辑，不新增 LTO 专用接口。

操作成功后刷新素材模板详情，重新渲染 LTO 列表。

---

# 4. 校验与异常

| 场景 | 提示 |
| --- | --- |
| 优惠文案为空 | `请输入优惠文案` |
| 优惠文案超过 16 字符 | `优惠文案不能超过 16 个字符` |
| ABSOLUTE 未选时间 | `请选择到期时间` |
| ABSOLUTE 时间早于当前时间 | `到期时间必须晚于当前时间` |
| ABSOLUTE 未选时区 | `请选择时区` |
| RELATIVE 时长为空 | `请输入有效时长` |
| RELATIVE 时长 <= 0 | `时长必须大于 0` |
| 组合选择 LTO 且选择 FOOTER | `限时优惠模板不支持 FOOTER` |
| LTO `hasExpiration=true` 且无按钮素材 | `展示到期时间的限时优惠组合必须选择按钮素材` |
| 第一个按钮不是 COPY_CODE | `限时优惠组合的第一个按钮必须为 COPY_CODE` |
| COPY_CODE.example 为空 | `COPY_CODE 示例优惠码不能为空` |
| 优惠码超过 15 位 | `优惠码不能超过 15 个字符` |

后端错误按通用结构展示：

```json
{
  "code": "LTO_COPY_CODE_NOT_FIRST",
  "message": "限时优惠组合的第一个按钮必须为 COPY_CODE"
}
```

---

# 5. 联调与验收

## 5.1 联调顺序

1. 素材模板创建：新增 LTO 素材并保存。
2. 素材模板详情：刷新后回显 `limitedTimeOfferList`。
3. 人工组合：选择 LTO 并保存。
4. 组合查询：确认返回 `limitedTimeOfferMaterialId`。
5. 正式模板创建：确认 `components` 包含 LTO。
6. 后端创建通道模板：确认内部 `expirationPolicy` 不透传。

## 5.2 前端验收点

### 素材模板页

- [ ] LTO 区域位于模板头部和模板内容之间。
- [ ] 支持添加多个 LTO。
- [ ] 支持 LTO 预览、停止生成、停用。
- [ ] 优惠文案最大 16 字符。
- [ ] ABSOLUTE 必须选择未来时间和时区。
- [ ] RELATIVE 必须大于 0。
- [ ] 编辑时能正确回显 ABSOLUTE / RELATIVE。

### 人工组合页

- [ ] LTO 槽位位于头部和内容之间。
- [ ] 默认“无 LTO”。
- [ ] 选择 LTO 后 FOOTER 自动置灰且清空。
- [ ] 取消 LTO 后恢复 FOOTER 可选。
- [ ] `hasExpiration=true` 时校验 COPY_CODE 第一位。
- [ ] 保存请求携带正确的 `limitedTimeOfferMaterialRef`。

### 正式模板

- [ ] `components` 包含 `LIMITED_TIME_OFFER`。
- [ ] 无 LTO 组合生成的模板不包含 LTO。
- [ ] LTO 组件字段与接口示例一致。

## 5.3 回归点

| 场景 | 期望 |
| --- | --- |
| 旧素材模板无 LTO | 页面和保存行为不变 |
| 旧人工组合无 LTO | 正式模板生成行为不变 |
| OTP 模板 | 不受影响 |
| UTILITY 模板 | 不受影响 |
| 普通 MARKETING 模板 | 不受影响 |
| COPY_CODE 但无 LTO | 现有逻辑不变 |
